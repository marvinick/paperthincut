<%= simple_form_for(@calendar, html: {class: "form-item"}) do |f| %>
  <%= f.error_notification %>)
    <%= f.input :month, required: true, autofocus: true, label: false, placeholder: "month", input_html: {class: "form-control"} %>
    <%= f.button :submit, "Submit", class: "btn btn-primary btn-block" %>
    <% end %>